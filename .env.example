# 前端環境變數範例 (不要提交帶有密碼或私密內容的 .env)
# Vite 只會注入以 VITE_ 開頭的 env 到前端程式碼

# 直接指定完整的 WebSocket URL (優先使用)
# 範例: VITE_BACKEND_WS=ws://localhost:3000
VITE_BACKEND_WS=ws://localhost:3000

# 或者指定 host:port，程式會依當前 protocol 決定 ws/wss
# 範例: VITE_BACKEND_HOST=backend.example.com:3000
#VITE_BACKEND_HOST=localhost:3000
